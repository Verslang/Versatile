external function ExitProcess(uExitCode: u32) -> void

function main() -> i32 {
    declare sum: i32 = 0
    for (declare i: i32 = 0; i < 10; i = i + 1) {
        sum = sum + i
    }
    return sum
}
